<nav class="border-b border-slate-200 bg-white/80 backdrop-blur">
  <div class="mx-auto flex max-w-6xl items-center justify-between px-6 py-4">
    <div class="flex items-center gap-3">
      <div class="flex h-10 w-10 items-center justify-center rounded-2xl bg-indigo-600 text-white font-semibold">
        OSS
      </div>
      <div>
        <p class="text-sm uppercase tracking-[0.3em] text-indigo-500">Support Portal</p>
        <p class="text-lg font-semibold">Status & Communication</p>
      </div>
    </div>
    <div class="flex items-center gap-3 text-sm font-medium">
      <a class="rounded-full px-4 py-2 hover:bg-slate-100" href="/dashboard">Dashboard</a>
      <% if (currentUser && currentUser.role === 'admin') { %>
        <a class="rounded-full px-4 py-2 hover:bg-slate-100" href="/admin">Admin</a>
      <% } %>
      <% if (currentUser) { %>
        <form action="/logout" method="POST">
          <button class="rounded-full bg-slate-900 px-4 py-2 text-white hover:bg-slate-800" type="submit">
            Logout
          </button>
        </form>
      <% } else { %>
        <a class="rounded-full bg-indigo-600 px-4 py-2 text-white hover:bg-indigo-500" href="/login">Login</a>
      <% } %>
    </div>
  </div>
</nav>
